interface:
  display_name: "BotsArgue"
  short_description: "Resolve coworkers’ existing separately controlled agent dispute"
  default_prompt: "Use $botsargue only because my existing coding-agent session and my coworker’s separately controlled existing session already reached conflicting technical conclusions."
